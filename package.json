{
  "name": "malaria-diagnosis-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "backend": "uvicorn backend.main:app --reload",
    "start": "concurrently \"npm run backend\" \"npm run dev\"",
    "test": "vitest"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "concurrently": "^9.2.1",
    "jsdom": "^29.1.1",
    "vite": "^8.0.8",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "mailto": "^0.1.2"
  }
}
